import { PageLoadingState } from "@/components/research/ui";

export default function Loading() {
  return (
    <PageLoadingState
      title="Loading saved research"
      body="Opening the saved product research and rebuilding the analysis view."
    />
  );
}
