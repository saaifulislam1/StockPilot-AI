services:
  postgres:
    image: postgres:16-alpine
    container_name: profitresearch-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: profitresearch
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - profitresearch_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d profitresearch"]
      interval: 5s
      timeout: 5s
      retries: 12

volumes:
  profitresearch_postgres_data:
